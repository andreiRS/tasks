{
  "name": "tasks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "tasks": "./src/cli.ts"
  },
  "scripts": {
    "tasks": "bun run src/cli.ts",
    "test": "bun test",
    "build": "bun build --compile --target=bun ./src/cli.ts --outfile=dist/tasks"
  },
  "dependencies": {
    "ink": "^7.0.4",
    "react": "^19.0.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.0.0",
    "typescript": "^5.6.0"
  }
}
